{% extends 'base.html.twig' %}

{% block title %}Aéroport{% endblock %}

{% block body %}
    <h1>Aéroport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ airport.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ airport.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_airport_index') }}">Retour à la liste</a>

    <a href="{{ path('app_airport_edit', {'id': airport.id}) }}">Modifier</a>

    {{ include('airport/_delete_form.html.twig') }}
{% endblock %}
