{% extends 'base.html.twig' %}

{% block title %}Airport{% endblock %}

{% block body %}
    <h1>Airport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ airport.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ airport.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_airport_index') }}">back to list</a>

    <a href="{{ path('app_airport_edit', {'id': airport.id}) }}">edit</a>

    {{ include('airport/_delete_form.html.twig') }}
{% endblock %}
