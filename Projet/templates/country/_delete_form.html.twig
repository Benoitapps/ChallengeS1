<form method="post" action="{{ path('app_country_delete', {'id': country.id}) }}" onsubmit="return confirm('Etes vous sûr que vous souhaitez supprimer cet objet ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ country.id) }}">
    <button class="btn">Supprimer</button>
</form>
