{{ form_start(form) }}
<h3>Ajouter un nouveau pays</h3>
    {{ form_widget(form) }}
<button class="btn">{{ button_label|default('Enregistrer') }}</button>
<button class="btn-annuler"><a id="annuler" href="{{ path('app_airport_index') }}">Annuler</a></button>
{{ form_end(form) }}
