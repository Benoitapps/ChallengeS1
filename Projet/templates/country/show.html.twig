{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ country.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_country_index') }}">Retour à la liste</a>

    <a href="{{ path('app_country_edit', {'id': country.id}) }}">Modifier</a>

    {{ include('country/_delete_form.html.twig') }}
{% endblock %}
