{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>N°</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ annonce.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_annonce_index') }}">Retour à la liste</a>

    <a href="{{ path('app_annonce_edit', {'id': annonce.id}) }}">Modifier</a>
    <a href="{{ path('app_annonce_paiment', {'id': annonce.id}) }}">Payer</a>

    {{ include('annonce/_delete_form.html.twig') }}
{% endblock %}
