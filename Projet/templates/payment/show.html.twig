{% extends 'base.html.twig' %}

{% block title %}Payment{% endblock %}

{% block body %}
    <h1>Payment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment.id }}</td>
            </tr>
            <tr>
                <th>NumCarte</th>
                <td>{{ payment.NumCarte }}</td>
            </tr>
            <tr>
                <th>Expiration</th>
                <td>{{ payment.expiration ? payment.expiration|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Cvv</th>
                <td>{{ payment.cvv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_payment_index') }}">back to list</a>

    <a href="{{ path('app_payment_edit', {'id': payment.id}) }}">edit</a>

    {{ include('payment/_delete_form.html.twig') }}
{% endblock %}
