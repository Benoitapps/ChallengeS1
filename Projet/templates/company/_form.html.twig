{{ form_start(form) }}
<h3>Ajouter votre entreprise </h3>
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Ajouter') }}</button>
    <button class="btn-annuler"><a id="annuler" href="{{ path('app_company_index') }}">Annuler</a></button>
{{ form_end(form) }}

